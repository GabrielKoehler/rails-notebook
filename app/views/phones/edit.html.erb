<h1><%= "#{t('messages.editing')} #{@phone.class.model_name.human(count: 1 )}" %></h1>

<%= render 'form', phone: @phone %>

<%= link_to t('buttons.show'), @phone %> |
<%= link_to t('buttons.back'), phones_path %>
