<h1><%= "#{t('messages.editing')} #{@address.class.model_name.human(count: 1 )}" %></h1>

<%= render 'form', address: @address %>

<%= link_to t('buttons.show'), @address %> |
<%= link_to t('buttons.back'), addresses_path %>
